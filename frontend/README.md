# CarFlip OS — frontend

Next.js (App Router) + TypeScript + Tailwind. See
[`../docs/README.md`](../docs/README.md) for architecture and environment
variables.

```bash
cp .env.example .env.local
npm install
npm run dev   # http://localhost:3000, expects the API from /backend running
```
